import { UnderlineMatchPipe } from './underline-match.pipe';

describe('UnderlineMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new UnderlineMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
